// Дополни код в редакторе так, чтобы он выводил в консоль числа
// от 1 до N включительно. Для этого вместо комментария /* stop condition */
// напиши верное условие остановки цикла.

function printNumbers(N) {
  for (let i = 1; i <= N; i++) {
    console.log(i);
  }
}

printNumbers(10);
